"""Shared library for the BioMe cancer-risk pipeline.

Everything cancer- and cohort-specific is read from config/*.yaml at runtime.
No column name, path, code set, or parameter is hardcoded here — see CLAUDE.md.
"""
